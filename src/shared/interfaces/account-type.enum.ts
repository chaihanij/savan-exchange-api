export enum AccountTypeEnum {
  Root = 'root', // Root account with full access
  System = 'system', // System account with limited access
  Admin = 'admin', // Administrator account with elevated privileges
  Staff = 'staff', // Staff account with standard access
  Customer = 'customer', // Customer account with basic access
  Partner = 'partner', // Partner account with specific access
}
